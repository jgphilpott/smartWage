name: Cairo Proofs

on:
  push:
    paths:
      - 'cairo/**'
      - '.github/workflows/cairo-proofs.yml'
  pull_request:
    paths:
      - 'cairo/**'
      - '.github/workflows/cairo-proofs.yml'

jobs:
  build-and-smoke-test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: cairo

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Scarb
        uses: software-mansion/setup-scarb@v1

      - name: Build Cairo package
        run: scarb build

      - name: Execute employment duration example
        run: |
          # Other example inputs require real Poseidon commitments and are
          # documented placeholders, so this smoke test runs the fully-runnable
          # non-placeholder example to catch execution regressions.
          cairo-run \
            --program target/dev/example_employment_duration_proof \
            --layout all_cairo \
            --program-input examples/inputs/employment_duration_input.json
